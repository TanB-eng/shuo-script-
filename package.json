{
  "name": "grasp-rat-bot",
  "version": "0.1.0",
  "description": "囤囤鼠历险记 拾金生存 bot（直接协议客户端，无浏览器自动化）",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test",
    "bridge": "node src/bot-bridge.js",
    "bridge:observe": "node src/bot-bridge.js --observe",
    "check": "node --check src/bot-bridge.js && node --check src/bridge.js && node --check src/config.js && node --check src/cooldown.js && node --check src/state.js && node --check src/strategy.js && node --check src/logger.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
